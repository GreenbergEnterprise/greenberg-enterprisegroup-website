# Copy to .env.local and fill in when you wire up the database later.
# MongoDB is not used yet — these are placeholders for when a database is added.
# MONGODB_URI="mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority"
# MONGODB_DB="greenberg"
